Phase 1: Solid MVP frontend (what users see)

Goal: clickable, coherent product shell.

Authentication / access
	•	Replace “Request access” email form with “Sign in with Twitch” button
	•	Button can be a stub for now (just console.log("oauth"))
	•	Remove email-only access flow (it’s a distraction)

Pages (finish, don’t perfect)
	•	Dashboard Home
	•	Metrics only
	•	Pill selector (last stream / week / month)
	•	Streams page
	•	Embedded Twitch VOD (you already did this)
	•	Placeholder list of streams (fake data OK)
	•	Vibe Check page
	•	Keep current placeholder
	•	Sidebar
	•	Icons + labels working
	•	Collapsed state looks clean

At the end of this phase:

You can demo the app without apologizing.

⸻

Phase 2: Minimum backend to feel “real”

Goal: one real integration, not ten fake ones.

Backend setup
	•	Create backend repo (FastAPI is fine)
	•	Health endpoint: GET /health
	•	Simple config for Twitch credentials

Twitch OAuth (single path)
	•	/auth/twitch/start
	•	/auth/twitch/callback
	•	Store twitch_user_id, login, display_name

No gating yet. Just login.

⸻

Phase 3: One killer feature

Goal: one thing that makes Streamers Edge interesting.

Pick one:

Option A (recommended)
	•	Fetch last N VODs for logged-in user
	•	Compute:
	•	total messages
	•	average sentiment (even dummy)
	•	Display metrics on Dashboard Home

Option B
	•	Identify “most negative chatter” for a single VOD
	•	Show it on Vibe Check page

Do NOT try both yet.

⸻

Phase 4: Access gating (only after value exists)

Goal: control growth without blocking yourself.
	•	After Twitch login:
	•	Fetch last 10 VODs
	•	Compute median view_count
	•	If below threshold:
	•	Show “We’ll reach out soon”
	•	Still store user
	•	If above:
	•	Grant access flag

Important: soft gate, not hard rejection.

⸻

Phase 5: Polish only if energy remains

Optional, not required.
	•	Persist time range in URL
	•	Skeleton loaders
	•	Tooltips in collapsed sidebar
	•	Basic error states
